# Desenvolvimento rápido: sem DATABASE_URI o projeto usa SQLite (db.sqlite3).
DATABASE_URI=

# PostgreSQL: preencha DATABASE_URI para usar ambiente local mais próximo da produção.
# Exemplo:
# DATABASE_URI=postgresql://usuario:senha@localhost:5432/fincontrol
